# Домашнее задание 2
## Улучшения + трекинг экспериментов + абляции (подробно)

### Цель
На основе baseline (базового решения) из ДЗ1 необходимо:
- внести 1–2 осмысленных улучшения, которые могут потенциально улучшить качество модели (каждое изменение должно быть обосновано гипотезой и связано с анализом исходных результатов);
- организовать подробный трекинг (отслеживание) всех экспериментов — фиксировать, что и почему изменяли;
- с помощью абляций (отключение/убирание отдельных компонентов) показать вклад каждого улучшения.

---

## Требования

### 1. Улучшения по сравнению с baseline (обязательно)
Сделать 1–2 осмысленных, обоснованных улучшения вашего базового ML-решения.

**Примеры улучшений:**
- добавление новых признаков или их преобразование (feature engineering, например: логарифмирование признаков, отбросить выбросы, внедрить дополнительные агрегированные признаки);
- изменение/усложнение preprocessing (например, нормализация, one-hot кодирование для категорий);
- переход на другую модель (избегать чрезмерно сложных моделей на малых данных или без необходимости);
- введение регуляризации (например, L1, L2 или dropout для снижения переобучения);
- балансировка классов (undersampling/oversampling, class weights);
- простые виды аугментации (например, поворот изображений, добавление шума в данные).

**Важно:**
- Каждый шаг обосновывается гипотезой (“Мы считаем, что... поэтому пробуем ...” — фиксировать гипотезу в отчёте).
- Запрещено “бесцельное тыканье и перебор параметров”. Только продуманные изменения.

---

### 2. Трекинг экспериментов (обязательно)
Вести подробную историю всех экспериментов. Это может быть в виде:
- таблички (например, в Markdown с аккуратным отчетом по экспериментам) или использованием систем типа W&B, MLflow **(предпочтительно, но я понимаю, что обстановка сложная и доступа может не быть)**;
- в каждом эксперименте фиксируется:
  - **конфигурация:** что именно было изменено по сравнению с baseline;
  - **метрики** (например, accuracy, F1, ROC-AUC — в зависимости от задачи, значение метрик должно быть одинаковым для всех экспериментов);
  - **seed** (значение random seed, если воспроизводимость критична — записывать обязательно).
  - хорошо, если в рамках запуска будут **визуальные артефакты** - они улучшают читаемость



Чем подробнее запись, тем проще будет анализ. Желательно добавить колонку “гипотеза” для каждого эксперимента.

---

### 3. Абляции (минимум 2)
Нужно провести не менее двух абляционных экспериментов: убирать/отключать по одному компоненту (новый признак, регуляризация, аугментация и т.п.), чтобы показать вклад каждого улучшения.

**Примеры абляций:**
- убрать отдельный новый признак и оценить изменение метрики;
- убрать регуляризацию и увидеть, ухудшается ли качество;
- убрать аугментацию.

В конце нужно написать **краткое резюме**, где вы подытоживаете абляционную работу и проговариваете что дало прирост и почему. 
В т.ч. ратко опишите, какой вывод вы сделали по каждой абляции.

---

### 4. Честное сравнение

Ожидается подробное описание того, как обеспечивалась честность сравнения между экспериментами.

**Что обязательно должно быть одинаковым для всех экспериментов:**
- Один и тот же train/validation split (одна и та же разбивка данных! Разделяйте заранее и используйте фиксированный seed для разбиения).
- Одна и та же метрика (например, всегда accuracy или F1 — НЕ меняйте её по ходу работы).
- Одни и те же этапы предварительной обработки (preprocessing) данных.
- Никакого подбора гиперпараметров “на тесте” или по leaderboard (тест можно использовать только для финального отбора, не для настройки!).

**Распишите, как вы фиксировали и контролировали эти условия. Как убедились в отсутствии утечек (data leakage)? Как исключили “подгон” под тест или обмен данными между train и val?** Приведите технику и мышление.

---

### 5. Выводы
В конце дайте краткий, но содержательный анализ:
- Какие изменения дали прирост качества? Почему по-вашему это произошло?
- Какие улучшения не сработали и чему вас это научило?
- Какие эффекты оказались шумом? Опишите кратко возможные причины.

---

### 6. Обязательные артефакты
В работе **обязательно** должны присутствовать следующее:
- Таблица экспериментов (см. выше, с подробным описанием каждого шага).
- Графики (например: метрика по экспериментам, кривые learning curve, лог Loss/Accuracy по эпохам).
- Визуализации, если подходит для вашей задачи: например, confusion matrix, распределение ошибок, plot признаков, примеры ошибок модели.
- Сохранённые результаты (логи, predictions, чекпоинты — опционально, но стандартные CSV с предсказаниями и параметрами экспериментов обязательно полезны).

**Артефакты — обязательная часть оценки.** Без них работа считается неполной.

---

## Критерии оценки

| Критерий                  | Баллы |
|---------------------------|-------|
| Осмысленные улучшения     | 6     |
| Трекинг экспериментов     | 5     |
| Абляции (≥2)              | 6     |
| Честное сравнение         | 2     |
| Выводы                    | 1     |

---

## Минимум для зачёта
- Базовая (baseline) и хотя бы одна улучшенная версия.
- История (таблица) экспериментов.
- Минимум 2 абляции.
- Краткий анализ результатов (какие улучшения “сыграли”, какие нет).
- Есть базовые артефакты: таблица экспериментов + минимум одна визуализация.

---

# Домашнее задание 3
## Анализ ошибок + robustness checks + ограничения (подробно)

---

## Цель
Понять глубже:
- где и почему модель ошибается (наиболее типичные проблемы);
- насколько результаты модели стабильны к изменению условий (устойчивость, robust-испытания);
- где возможны границы применимости решения.

---

## Требования

### 1. Анализ ошибок (обязательно)
Необходимо найти и показать **3–5 типовых ошибок модели**: примеры, где модель допускает промах, и дать объяснения этим ошибкам.

**Примеры типов ошибок:**
- Ошибки на редких классах (например, класс составляет 2% данных — как модель справляется с такими примерами?).
- Ошибки из-за шумной разметки (очевидно некорректные/двусмысленные лейблы в train/val).
- Ошибки на сложных, неоднозначных примерах (например, объект на границе двух классов).
- Ошибки на выбросах (outliers).

**Рекомендуется:**
- Привести реальные примеры из датасета с пояснением.
- Для каждой ошибки кратко объяснить, ПОЧЕМУ (гипотетически) модель тут ошибается.

---

### 2. Срезы / диагностика (минимум 2)
Проведите диагностику по **разным интересным подгруппам/срезам**. Например, разберите разные категории, интервалы признаков, длину входа, временные интервалы и т.д.

**Примеры анализируемых срезов:**
- Длина текста (короткие, средние, длинные);
- По классам (какие классы хуже/лучше предсказываются?);
- Группы по значениям признака (например, возраст <30, 30–60, >60);
- Валидация на новых временных отсечках.
- Приведите таблицу результатов.
- Кратко опишите заметные различия между группами и дайте объяснение.

---

### 3. Robustness checks (минимум 1–2)
Проведите 1–2 проверки устойчивости модели (robustness):

**Варианты:**
- измените seed (разные random splits — насколько меняется метрика?);
- добавьте шум к входным данным (например, Gaussian noise, небольшое искажение — и посмотрите, сильно ли падает качество);
- уменьшите размер обучающей выборки (проверьте метрику на 100%, 50%, 20% train);
- поменяйте preprocessing (уберите normalization и посмотрите эффект).


- Рекомендуется указать заметные эффекты и, если возможно, визуализировать зависимость метрики от выбранного условия.

---

### 4. Ограничения модели
Обязательно развернуто опишите:
- В каких случаях модель не работает или работает хуже (в каких условиях, группах данных, с какими задачами).
- Основные возможные источники ошибок (например, в данных, в постановке задачи, в недостаточном объеме обучающей выборки).
- Потенциальные риски применения модели на практике (например — “модель может проявлять предвзятость на малых группах”, “не подходит для медицинских задач без отдельной валидации” и пр.).
- Возможные источники утечек (data leakage) и как вы их контролировали.

---

### 5. Обязательные артефакты
Без артефактов работа засчитана не будет.

**Обязательно предоставить:**
- Примеры ошибок модели (с пояснением — почему они возникли).
- Визуализации по результатам анализа (например, confusion matrix, распределения ошибок по группам, plot метрик по разным условиям).
- Таблицы по срезам.
- Результаты robustness checks (отдельно или в той же таблице).

---

## Критерии оценки

| Критерий             | Баллы |
|----------------------|-------|
| Анализ ошибок        | 7     |
| Срезы                | 4     |
| Robustness checks    | 5     |
| Ограничения          | 3     |
| Оформление           | 1     |

---

## Минимум для зачёта
- Показаны типовые ошибки работы модели с объяснениями.
- Проведён и описан хотя бы один тест устойчивости к изменению условий.
- Явно сформулированы ограничения и риски применения.
- Присутствуют артефакты (визуализации, таблицы по группам, разборы ошибок).

---

# Общие требования

- Вся работа строится на базе baseline из ДЗ1 с прозрачным описанием изменений.
- Код воспроизводим и снабжен инструкцией по запуску.
- Все эксперименты должны быть повторяемыми (фиксируйте seed/random_state, конфиги).
- Утверждения и выводы делаются на основании данных, а не только на “ощущениях/интуиции” (хотя гипотезы по найтию это нормально и иногда хорошо - "наитие такое появилось что мне нужно выполнить определенный алгоритм")

---

# Критерии сильной работы

- Для каждого изменения описана гипотеза, эксперимент и сделан вывод.
- Все сравнения между решениями честные/корректные.
- Есть как минимум одна хорошо оформленная визуализация и одна таблица.
- Приведены как удачные, так и неудачные попытки — явно указано, что из изменений НЕ сработало и какие уроки были извлечены.